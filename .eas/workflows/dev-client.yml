name: Dev Client Build
jobs:
  build_ios_dev:
    type: build
    params:
      platform: ios
      profile: development
  build_android_dev:
    type: build
    params:
      platform: android
      profile: development
