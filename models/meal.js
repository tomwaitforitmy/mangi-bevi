class Meal {
  constructor(title, id, imageUrl, ingredients, steps) {
    this.title = title;
    this.id = id;
    this.imageUrl = imageUrl;
    this.ingredients = ingredients;
    this.steps = steps;
  }
}

export default Meal;
