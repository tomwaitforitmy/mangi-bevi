class Meal {
  constructor(title, id, primaryImageUrl, ingredients, steps, imageUrls) {
    this.title = title;
    this.id = id;
    this.primaryImageUrl = primaryImageUrl;
    this.ingredients = ingredients;
    this.steps = steps;
    this.imageUrls = imageUrls;
  }
}

export default Meal;
