class MovableData {
  constructor(id, title, height, order, position, threshold) {
    this.id = id;
    this.title = title;
    this.height = height;
    this.order = order;
    this.position = position;
    this.threshold = threshold;
  }
}

export default MovableData;
