My top requirements are:

1. No regression: Everything regarding works with expo router. All screens are reachable as before, all required parameters are passed somehow into the screens as before. Absolute must have prio 1.
2. No rendering regression: Top Header renders nicely, no text is cut off, all screens use the mobile display complete. No hidden items. Prio 2, but hard to test without manuel review.
3. The bottom tab navigation supports liquid glass now. Expo router offers better/easier means to integrate new Apple liquid glass bottom tab navigation on iOS. This would be prio 2, but still a goal to reach. Maybe with second feature run. Tell me, if not wise with first iteration.
4. Nice to have: Deep links to screens are possible. Prio 3. Totally optional
5. Nice to have: Dark mode feature. Prio 3. Totally optional.

Use the full spec-kit workflow to implement the expo router migration. Attempt to do everything without my explicit feedback. You have all required rights in this project folder, for web research and for running code changes and/or tests locally. If have questions needing clarification, raise them as soon as possible.
You may commit partial progress when it makes sense and is well tested.

Start unsupervised implementation.
